// Re-exported from the shared package so the web fallback and the Worker mock
// stay in lockstep. See packages/shared/src/sampleArticles.ts.
export { sampleArticles as mockArticles } from "@euronews/shared";
